<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to 'Todo Task', root_path, class: "text-decoration-none navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'User', users_path , class:"nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Task
          </a>
          <ul class="dropdown-menu">
            <% if logged_in? %>
              <li><%= link_to 'Create Task', new_task_path , class:"dropdown-item" %></li>
            <% end %>
            <li><%= link_to 'Tasks List', tasks_path , class:"dropdown-item" %></li>
          </ul>
        </li>
        <% if logged_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= "(Admin) " if (current_user.admin) %> Profile [<%= current_user.username %>]
            </a>
            <ul class="dropdown-menu">
              <li> <%= link_to 'View Your Profile', user_path(current_user) , class:'dropdown-item' %></li>
              <li> <%= link_to 'Edit Your Profile', edit_user_path(current_user) , class:'dropdown-item' %></li>
              <li> <%= link_to 'Delete Your Account', user_path(current_user) , data: { turbo_method: :delete, turbo_confirm: "Are you sure want to delete your account" }, class:'dropdown-item text-danger' %></li>
            </ul>
          </li>
          <!-- <li class="nav-item"> -->
          <%#= link_to (current_user.username), user_path(current_user),  class:'nav-link active' %>
          <!-- </li> -->
          <li class="nav-item">
            <%= link_to 'Log out', logout_path, data:{ turbo_method: :delete, turbo_confirm: "Are you sure to logout?" }, class:'nav-link active' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log in', login_path, class:'nav-link active' %></li>
          <li class="nav-item">
            <%= link_to 'Sign up', signup_path, class:'nav-link active' %>
          </li>
        <% end %>
      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
